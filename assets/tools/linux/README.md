Place Linux tool binaries here before packaging:

- `yt-dlp`
- `ffmpeg`
- `ffprobe`

Fetchdeck copies the current platform's bundled tools into the app support
directory on startup before executing them.
